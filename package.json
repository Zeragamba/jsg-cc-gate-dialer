{
  "name": "ssgd",
  "version": "0.2.1",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "fix": "npm-run-all -pl \"fix:*\"",
    "fix:prettier": "yarn prettier --write",
    "prettier": "prettier ."
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "@prettier/plugin-lua": "^0.0.3",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.5.3"
  },
  "prettier": {
    "plugins": [
      "@prettier/plugin-lua"
    ]
  }
}
